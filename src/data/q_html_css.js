// HTML Questions — 12 fresher, 12 junior, 12 mid
export const HTML_QUESTIONS = [
  // ── FRESHER ──────────────────────────────────────────────────────────────
  { id:'html-f1', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between HTML elements and HTML tags?', answer:'A tag is the markup written in angle brackets, e.g. <p> and </p>. An element is the complete unit — opening tag + content + closing tag. Self-closing tags like <img /> and <br /> form a complete element on their own.' },
  { id:'html-f2', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between HTML and HTML5?', answer:'HTML5 (2014) added semantic elements (<header>,<footer>,<article>,<nav>,<section>), native <audio>/<video>, <canvas> for 2D drawing, localStorage/sessionStorage, new input types (email, date, range, color), Geolocation API, and WebSockets. It also deprecated presentational tags like <font> and <center>.' },
  { id:'html-f3', experienceLevel:'fresher', type:'conceptual', question:'What is the purpose of the DOCTYPE declaration?', answer:'<!DOCTYPE html> tells the browser to render in standards mode (HTML5). Without it, browsers enter "quirks mode" and apply legacy rendering rules that differ per browser, breaking modern layouts.', code:'<!DOCTYPE html>\n<html lang="en">\n  <head><title>My Page</title></head>\n  <body><h1>Hello</h1></body>\n</html>' },
  { id:'html-f4', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between block-level and inline elements?', answer:'Block elements start on a new line and take full parent width: <div>, <p>, <h1>–<h6>, <ul>, <section>. Inline elements flow within text and only take content width: <span>, <a>, <img>, <strong>, <em>, <input>. display:inline-block gives block sizing while staying inline.' },
  { id:'html-f5', experienceLevel:'fresher', type:'conceptual', question:'What are semantic HTML elements and why do they matter?', answer:'Semantic elements describe their purpose: <header>, <nav>, <main>, <article>, <section>, <aside>, <footer>. They help screen readers navigate (accessibility/WCAG), help search engines understand page structure (SEO), and make code readable and maintainable.' },
  { id:'html-f6', experienceLevel:'fresher', type:'practical', question:'How do you create an accessible form with email, password, and submit?', answer:'Use <label> with matching for/id, appropriate type attributes, and required for validation.', code:'<form action="/login" method="POST">\n  <label for="email">Email</label>\n  <input type="email" id="email" name="email" required />\n  <label for="password">Password</label>\n  <input type="password" id="password" name="password" required minlength="8" />\n  <button type="submit">Sign In</button>\n</form>' },
  { id:'html-f7', experienceLevel:'fresher', type:'conceptual', question:'What is the alt attribute on <img> and why is it important?', answer:'alt provides alternative text shown when an image fails to load and read by screen readers. It is essential for: accessibility (WCAG 2.1 requires it), SEO (search engines index it), and UX when images are blocked or slow.' },
  { id:'html-f8', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between the GET and POST methods in an HTML form?', answer:'GET appends data to the URL as query strings — visible in browser history, bookmarkable, max ~2000 chars, never for sensitive data. POST sends data in the request body — not visible in URL, required for passwords, file uploads, and large payloads.' },
  { id:'html-f9', experienceLevel:'fresher', type:'output', question:'What is the rendered result of this markup?', code:'<p>I <strong>love</strong> <em>coding</em></p>', output:'"love" appears bold, "coding" appears italic, all on one line inside a paragraph.', explanation:'<strong> = bold with semantic importance. <em> = italic with semantic emphasis. Both are inline, so they render within the paragraph flow.', choices:[{label:'"love" bold, "coding" italic in one line',correct:true},{label:'All text bold',correct:false},{label:'Three separate paragraphs',correct:false},{label:'Error — cannot nest inline tags',correct:false}] },
  { id:'html-f10', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between <section>, <article>, and <div>?', answer:'<div> is a generic, non-semantic container. <section> groups thematically related content that belongs to a larger page (e.g. chapters). <article> is self-contained, independently distributable content like a blog post, news item, or comment.' },
  { id:'html-f11', experienceLevel:'fresher', type:'practical', question:'How do you open a link in a new tab securely?', answer:'Use target="_blank" with rel="noopener noreferrer". Without rel, the new page can access window.opener, enabling tabnapping phishing attacks.', code:'<a href="https://example.com" target="_blank" rel="noopener noreferrer">Visit</a>' },
  { id:'html-f12', experienceLevel:'fresher', type:'output', question:'What does this meta tag do?', code:'<meta name="viewport" content="width=device-width, initial-scale=1.0">', output:'Makes the page responsive. Sets the viewport width to the device screen width at 1:1 zoom, preventing mobile browsers from rendering at desktop width and scaling down.', explanation:'Without this, mobile sites appear zoomed-out. Required for responsive CSS media queries to work correctly.', choices:[{label:'Makes the page mobile-responsive',correct:true},{label:'Sets background color',correct:false},{label:'Adds a page icon',correct:false},{label:'Enables dark mode',correct:false}] },

  // ── JUNIOR ──────────────────────────────────────────────────────────────
  { id:'html-j1', experienceLevel:'junior', type:'conceptual', question:'What are ARIA roles and when should you use them?', answer:'ARIA (Accessible Rich Internet Applications) adds accessibility semantics to elements. Use it only when native HTML semantics are insufficient. Rule 1 of ARIA: use native HTML first. Examples: role="dialog" on custom modals, aria-label on icon-only buttons, aria-live for dynamic updates.' },
  { id:'html-j2', experienceLevel:'junior', type:'conceptual', question:'What is the difference between async and defer on a <script> tag?', answer:'Both download scripts in parallel with HTML parsing. async executes immediately when downloaded, interrupting parsing — suited for independent scripts (analytics). defer executes after HTML parsing is complete in document order — suited for DOM-manipulating scripts. Neither blocks parsing during download.' },
  { id:'html-j3', experienceLevel:'junior', type:'conceptual', question:'What is the difference between localStorage, sessionStorage, and cookies?', answer:'localStorage: ~5MB, persists until explicitly cleared, not sent with requests. sessionStorage: ~5MB, cleared when tab closes, tab-isolated. Cookies: ~4KB, sent with every HTTP request (overhead), can be httpOnly (no JS access), Secure (HTTPS only), SameSite (CSRF protection), set expiry date.' },
  { id:'html-j4', experienceLevel:'junior', type:'practical', question:'How do you use data-* attributes and access them in JavaScript?', answer:'data-* attributes store custom data on elements. Access via dataset.camelCase in JS.', code:'<button data-product-id="42" data-action="add-to-cart">Add</button>\n\nconst btn = document.querySelector("button");\nconsole.log(btn.dataset.productId); // "42"\nconsole.log(btn.dataset.action);    // "add-to-cart"' },
  { id:'html-j5', experienceLevel:'junior', type:'conceptual', question:'What is the difference between <script type="module"> and a regular script?', answer:'Module scripts: have their own scope (no global pollution), support import/export, always deferred, strict mode by default, loaded once and cached, CORS-restricted. Regular scripts run in global scope, load synchronously unless async/defer.' },
  { id:'html-j6', experienceLevel:'junior', type:'conceptual', question:'What are Web Workers and when would you use them?', answer:'Web Workers run JavaScript on a background thread separate from the UI thread, communicating via postMessage(). Use for CPU-intensive tasks (image processing, complex calculations, parsing large datasets) that would otherwise freeze the UI. Workers cannot access the DOM.' },
  { id:'html-j7', experienceLevel:'junior', type:'output', question:'What is the difference in behaviour between these two inputs?', code:'<input type="text" />\n<input type="search" />', output:'type="search" adds a clear (×) button, triggers the search event on Enter, and carries accessibility semantics for search. Visually similar but semantically and functionally different.', explanation:'Use type="search" for search fields — screen readers announce it differently.', choices:[{label:'search shows clear button and fires search events',correct:true},{label:'Identical behaviour',correct:false},{label:'search disables autocomplete',correct:false},{label:'text validates email format',correct:false}] },
  { id:'html-j8', experienceLevel:'junior', type:'conceptual', question:'What is the purpose of <template> in HTML?', answer:'<template> holds inert HTML that is not rendered or executed when the page loads. The content is parsed and held in a DocumentFragment, cloned and inserted into the DOM dynamically with JavaScript. Used for reusable UI patterns without framework overhead.' },
  { id:'html-j9', experienceLevel:'junior', type:'practical', question:'How do you implement lazy loading for images in HTML?', answer:'Use the native loading="lazy" attribute on <img>. The browser defers loading until the image enters the viewport.', code:'<img src="hero.jpg" alt="Hero" loading="lazy" width="800" height="600" />\n<!-- Always set width/height to prevent layout shifts (CLS) -->' },
  { id:'html-j10', experienceLevel:'junior', type:'conceptual', question:'What is the difference between <link rel="preload"> and <link rel="prefetch">?', answer:'preload: fetches the resource immediately at high priority for the CURRENT page (fonts, critical CSS, hero images). prefetch: fetches at low priority for the NEXT expected navigation. preload has a must-use-within-a-few-seconds warning; prefetch is speculative.' },
  { id:'html-j11', experienceLevel:'junior', type:'conceptual', question:'What is the difference between <meter> and <progress> elements?', answer:'<progress> represents completion of a task (no defined range necessarily). <meter> represents a scalar measurement within a defined range (e.g. disk usage). <meter> supports min, max, low, high, optimum attributes for color-coded feedback.' },
  { id:'html-j12', experienceLevel:'junior', type:'practical', question:'How do you make a table accessible?', answer:'Use scope on <th>, caption element, and summary/aria-label on complex tables.', code:'<table>\n  <caption>Monthly Sales</caption>\n  <thead>\n    <tr>\n      <th scope="col">Month</th>\n      <th scope="col">Sales</th>\n    </tr>\n  </thead>\n  <tbody>\n    <tr><td>Jan</td><td>₹50,000</td></tr>\n  </tbody>\n</table>' },

  // ── MID ──────────────────────────────────────────────────────────────────
  { id:'html-m1', experienceLevel:'mid', type:'conceptual', question:'What is the Critical Rendering Path and how does HTML structure affect it?', answer:'CRP: Parse HTML → Build DOM → Parse CSS → Build CSSOM → Merge into Render Tree → Layout → Paint → Composite. Optimisations: inline critical CSS above-the-fold, defer/async non-critical JS, preload key fonts, use link rel="preconnect" for external origins, lazy-load below-fold images.' },
  { id:'html-m2', experienceLevel:'mid', type:'conceptual', question:'What are Web Components and what are their three main specifications?', answer:'1. Custom Elements — define new HTML tags (class MyEl extends HTMLElement). 2. Shadow DOM — encapsulated DOM + CSS scoping within a components shadow root. 3. HTML Templates — <template> and <slot> for reusable markup that is not rendered until cloned. Together they enable framework-agnostic reusable components.' },
  { id:'html-m3', experienceLevel:'mid', type:'conceptual', question:'How does the browser parse HTML when it encounters a render-blocking resource?', answer:'CSS is render-blocking — browser must fetch and build CSSOM before painting. Scripts without defer/async are parser-blocking — stop HTML parsing until downloaded and executed. Font loading blocks text rendering (FOIT/FOUT). Solutions: inline critical CSS, defer JS, font-display:swap, preload key resources.' },
  { id:'html-m4', experienceLevel:'mid', type:'conceptual', question:'What is the Content Security Policy (CSP) and how is it configured?', answer:'CSP is an HTTP response header or <meta> that restricts which sources can load scripts, styles, images etc. Prevents XSS by blocking inline scripts and unknown origins. Example: Content-Security-Policy: default-src self; script-src self cdn.example.com; style-src self unsafe-inline', code:'<meta http-equiv="Content-Security-Policy"\n      content="default-src \'self\'; script-src \'self\' cdn.jsdelivr.net">' },
  { id:'html-m5', experienceLevel:'mid', type:'conceptual', question:'What is the difference between Core Web Vitals — LCP, FID/INP, and CLS?', answer:'LCP (Largest Contentful Paint): loading — time for largest visible element. INP (Interaction to Next Paint, replaced FID): responsiveness — max delay from input to visual response. CLS (Cumulative Layout Shift): visual stability — unexpected layout movement. Google uses these for Search ranking.' },
  { id:'html-m6', experienceLevel:'mid', type:'output', question:'What does this <link> tag do and when should you use it?', code:'<link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>', output:'Pre-establishes a connection (DNS + TCP + TLS handshake) to fonts.googleapis.com before any resource is requested. Saves 100–500ms on first font request.', explanation:'Use preconnect for origins you know will be needed. crossorigin is needed for CORS font requests.', choices:[{label:'Pre-establishes DNS+TCP+TLS to the origin',correct:true},{label:'Loads all fonts from Google',correct:false},{label:'Blocks rendering until fonts load',correct:false},{label:'Only resolves DNS',correct:false}] },
  { id:'html-m7', experienceLevel:'mid', type:'conceptual', question:'What are resource hints and when do you use each?', answer:'preload: must-have current-page resource (critical CSS, hero image, fonts). prefetch: next-page resource at low priority. preconnect: pre-establish TCP+TLS to known origin. dns-prefetch: pre-resolve DNS only — lighter, use for many third-party origins. modulepreload: preload ES module graph.' },
  { id:'html-m8', experienceLevel:'mid', type:'conceptual', question:'How does the Shadow DOM provide style encapsulation?', answer:'Shadow DOM attaches an isolated DOM subtree to a host element. Styles inside the shadow root do not leak out; external styles do not leak in (except inherited properties and CSS custom properties). Web Components use this for reuse without style collisions. ::slotted() and :host() selectors cross the boundary.' },
  { id:'html-m9', experienceLevel:'mid', type:'practical', question:'How do you implement intersection observer for infinite scroll?', answer:'IntersectionObserver fires when a sentinel element enters the viewport.', code:'const sentinel = document.querySelector("#sentinel");\nconst observer = new IntersectionObserver((entries) => {\n  if (entries[0].isIntersecting) loadMore();\n}, { rootMargin: "200px" });\nobserver.observe(sentinel);' },
  { id:'html-m10', experienceLevel:'mid', type:'conceptual', question:'What is the difference between a progressive web app (PWA) and a regular web app?', answer:'PWAs use: Service Worker (background caching/offline), Web App Manifest (installable, splash screen, theme color), HTTPS (required). Features: offline-first, push notifications, background sync. Lighthouse audits PWA compliance. Not a separate build — same web tech with progressive enhancement.' },
  { id:'html-m11', experienceLevel:'mid', type:'conceptual', question:'What is the purpose of the <picture> element versus <img srcset>?', answer:'srcset provides resolution/size hints — browser CHOOSES best image. <picture> with <source> provides ART DIRECTION — explicitly switch images at breakpoints or based on media queries. Use srcset for same image at different resolutions; <picture> for entirely different crops per viewport.' },
  { id:'html-m12', experienceLevel:'mid', type:'output', question:'What happens when this form is submitted?', code:'<form>\n  <input type="text" name="q" value="hello">\n  <button type="submit">Go</button>\n</form>', output:'GET request to the current page URL with ?q=hello appended. Default form action is the current URL; default method is GET.', explanation:'Without action, the form submits to the current page. Without method, GET is used. The query string is appended to the URL.', choices:[{label:'GET request to current URL with ?q=hello',correct:true},{label:'POST to /submit',correct:false},{label:'Nothing — method required',correct:false},{label:'Refreshes page without submitting',correct:false}] },
];

// CSS Questions — 12 fresher, 12 junior, 12 mid
export const CSS_QUESTIONS = [
  // ── FRESHER ──────────────────────────────────────────────────────────────
  { id:'css-f1', experienceLevel:'fresher', type:'conceptual', question:'What is CSS specificity and how is it calculated?', answer:'Specificity determines which rule wins: Inline styles=1000, ID selectors=100, Class/attribute/pseudo-classes=10, Element/pseudo-elements=1. Sum the scores — highest wins. !important beats all. Equal specificity = last rule wins (cascade).' },
  { id:'css-f2', experienceLevel:'fresher', type:'conceptual', question:'Explain the CSS Box Model. What is the difference between content-box and border-box?', answer:'Every element = content + padding + border + margin. content-box (default): declared width applies to content only. Total = width + padding + border. border-box: width includes padding and border. Total = width. Use box-sizing:border-box globally (*, *::before, *::after) for predictable sizing.' },
  { id:'css-f3', experienceLevel:'fresher', type:'output', question:'What is the total rendered width of this element?', code:'.box {\n  width: 200px;\n  padding: 20px;\n  border: 5px solid;\n  box-sizing: content-box;\n}', output:'250px — content(200) + padding-left(20) + padding-right(20) + border-left(5) + border-right(5)', explanation:'content-box adds padding and border OUTSIDE the declared width. With border-box it would be exactly 200px.', choices:[{label:'250px',correct:true},{label:'200px',correct:false},{label:'220px',correct:false},{label:'245px',correct:false}] },
  { id:'css-f4', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between display:none, visibility:hidden, and opacity:0?', answer:'display:none: removed from layout entirely — no space, inaccessible to screen readers. visibility:hidden: hidden but space preserved — inaccessible. opacity:0: invisible but space preserved AND still responds to pointer events, still accessible. Use carefully.' },
  { id:'css-f5', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between position: static, relative, absolute, fixed, sticky?', answer:'static: default, document flow. relative: offset from own position, stays in flow. absolute: removed from flow, positioned relative to nearest positioned ancestor. fixed: relative to viewport, stays on scroll. sticky: in flow until scroll threshold, then fixed within scroll container.' },
  { id:'css-f6', experienceLevel:'fresher', type:'practical', question:'How do you centre a div both horizontally and vertically (3 methods)?', answer:'Flexbox, Grid, and absolute+transform are the three modern approaches.', code:'/* 1. Flexbox */\n.parent { display: flex; justify-content: center; align-items: center; }\n\n/* 2. Grid */\n.parent { display: grid; place-items: center; }\n\n/* 3. Absolute + transform */\n.child { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); }' },
  { id:'css-f7', experienceLevel:'fresher', type:'output', question:'What is the computed font-size of .child?', code:'html { font-size: 16px; }\n.parent { font-size: 1.5em; }\n.child { font-size: 1.5em; }', output:'36px — 16 × 1.5 = 24px (parent), then 24 × 1.5 = 36px (child). em compounds through inheritance.', explanation:'em is relative to the PARENT computed font-size, not the root. Use rem (root em) to avoid compounding.', choices:[{label:'36px (em compounds)',correct:true},{label:'24px',correct:false},{label:'16px',correct:false},{label:'1.5px',correct:false}] },
  { id:'css-f8', experienceLevel:'fresher', type:'practical', question:'How do you create and use a CSS custom property (variable)?', answer:'Declare with -- prefix on :root for global scope. Access with var(). Can have a fallback value.', code:':root {\n  --primary: #4f46e5;\n  --radius: 8px;\n}\n.button {\n  background: var(--primary);\n  border-radius: var(--radius);\n  /* Fallback: */\n  color: var(--text-color, #ffffff);\n}' },
  { id:'css-f9', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between margin and padding?', answer:'Padding is space INSIDE the border (between content and border) — takes the element background. Margin is space OUTSIDE the border (between elements). Margins collapse vertically — two adjacent vertical margins merge into the larger one. Padding does not collapse.' },
  { id:'css-f10', experienceLevel:'fresher', type:'conceptual', question:'What are pseudo-classes and pseudo-elements? Give examples.', answer:'Pseudo-classes style elements in a specific STATE: :hover, :focus, :nth-child(), :not(), :checked (single colon). Pseudo-elements style a specific PART of an element: ::before, ::after, ::first-line, ::placeholder, ::selection (double colon recommended).' },
  { id:'css-f11', experienceLevel:'fresher', type:'practical', question:'How do you create a CSS transition?', answer:'transition shorthand: property duration timing-function delay.', code:'.button {\n  background: #4f46e5;\n  transition: background 0.3s ease, transform 0.2s ease;\n}\n.button:hover {\n  background: #4338ca;\n  transform: translateY(-2px);\n}' },
  { id:'css-f12', experienceLevel:'fresher', type:'output', question:'Which color wins — and why?', code:'#box { color: red; }\n.text { color: blue; }\n<p id="box" class="text">Hello</p>', output:'red — the ID selector (#box, specificity=100) beats the class (.text, specificity=10).', explanation:'Regardless of order in the stylesheet, specificity hierarchy determines the winner. IDs beat classes.', choices:[{label:'red (ID beats class)',correct:true},{label:'blue (class is last)',correct:false},{label:'black (they cancel out)',correct:false},{label:'Depends on browser',correct:false}] },

  // ── JUNIOR ──────────────────────────────────────────────────────────────
  { id:'css-j1', experienceLevel:'junior', type:'conceptual', question:'What is CSS Grid and how does it differ from Flexbox?', answer:'Flexbox is one-dimensional — align items in a row OR column. CSS Grid is two-dimensional — control rows AND columns simultaneously. Use Flexbox for component internals (nav items, button groups). Use Grid for page-level layout (header + sidebar + main + footer). They complement each other.' },
  { id:'css-j2', experienceLevel:'junior', type:'practical', question:'How do you create a 3-column equal grid with a gap?', answer:'', code:'.grid {\n  display: grid;\n  grid-template-columns: repeat(3, 1fr);\n  gap: 24px;\n}\n/* Responsive — collapse to 1 col on mobile */\n@media (max-width: 768px) {\n  .grid { grid-template-columns: 1fr; }\n}' },
  { id:'css-j3', experienceLevel:'junior', type:'conceptual', question:'What is the difference between em, rem, vw, vh, and %?', answer:'em: relative to parent font-size (compounds). rem: relative to root (html) font-size — predictable. vw/vh: percentage of viewport width/height — good for full-screen layouts. %: relative to parent dimension. Use rem for typography, vw/vh for full-layout sections.' },
  { id:'css-j4', experienceLevel:'junior', type:'practical', question:'How do you implement a dark mode using CSS custom properties and prefers-color-scheme?', answer:'', code:':root { --bg: #ffffff; --text: #1e293b; }\n@media (prefers-color-scheme: dark) {\n  :root { --bg: #0f172a; --text: #f1f5f9; }\n}\n/* Toggle with JS class */\n[data-theme="dark"] { --bg: #0f172a; --text: #f1f5f9; }\nbody { background: var(--bg); color: var(--text); }' },
  { id:'css-j5', experienceLevel:'junior', type:'output', question:'What are the column widths for this grid at 600px total width?', code:'.container {\n  display: grid;\n  grid-template-columns: 1fr 2fr 1fr;\n  width: 600px;\n}', output:'150px | 300px | 150px — total fractions = 4. Each unit = 600/4 = 150px.', explanation:'fr units divide available space proportionally after fixed values and gaps are accounted for.', choices:[{label:'150px | 300px | 150px',correct:true},{label:'200px each',correct:false},{label:'100px | 400px | 100px',correct:false},{label:'Error',correct:false}] },
  { id:'css-j6', experienceLevel:'junior', type:'conceptual', question:'What is the stacking context and what creates a new one?', answer:'A stacking context is an independent layer in the z rendering order. Elements within a context stack relative to each other, not the global z-axis. Created by: position + z-index != auto, opacity < 1, transform, filter, will-change, isolation:isolate, mix-blend-mode. z-index only works within the same stacking context.' },
  { id:'css-j7', experienceLevel:'junior', type:'practical', question:'How do you create a responsive card grid that auto-fills based on minimum item width?', answer:'Use auto-fill with minmax for a truly responsive grid with no media queries.', code:'.grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));\n  gap: 20px;\n}' },
  { id:'css-j8', experienceLevel:'junior', type:'conceptual', question:'What is the difference between @keyframes animation and CSS transition?', answer:'Transitions: triggered by state change (hover/focus/class toggle), two states only, simpler. Animations (@keyframes): run automatically or on trigger, multiple keyframe steps, loop control, delay without needing state change, pauseable with animation-play-state.' },
  { id:'css-j9', experienceLevel:'junior', type:'conceptual', question:'What is will-change and when should you use it?', answer:'will-change hints to the browser that an element will be animated, prompting GPU layer promotion in advance. Use sparingly: only for elements you are about to animate, applied just before the animation starts, removed after. Overuse consumes GPU memory and hurts performance.' },
  { id:'css-j10', experienceLevel:'junior', type:'practical', question:'How do you truncate overflowing text with an ellipsis?', answer:'Single-line and multi-line truncation techniques.', code:'/* Single line */\n.truncate {\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n}\n/* Multi-line (2 lines, webkit) */\n.truncate-2 {\n  display: -webkit-box;\n  -webkit-line-clamp: 2;\n  -webkit-box-orient: vertical;\n  overflow: hidden;\n}' },
  { id:'css-j11', experienceLevel:'junior', type:'conceptual', question:'What is BEM naming methodology?', answer:'BEM (Block, Element, Modifier): Block = standalone component (.card). Element = part of block (.card__title, .card__image). Modifier = variant (.card--featured, .button--large). Avoids specificity wars by keeping all selectors at class level (specificity=10). Pairs well with CSS Modules.' },
  { id:'css-j12', experienceLevel:'junior', type:'output', question:'Is this animation GPU-accelerated?', code:'.slide {\n  animation: move 1s ease;\n}\n@keyframes move {\n  from { left: 0; }\n  to { left: 200px; }\n}', output:'NOT GPU-accelerated — animating left causes layout reflow on every frame. Use transform: translateX(200px) instead.', explanation:'Only transform and opacity are composited on the GPU by default. left/top/margin trigger layout.', choices:[{label:'Not GPU-accelerated — left causes layout reflow',correct:true},{label:'GPU-accelerated because animation is used',correct:false},{label:'Only in Chrome',correct:false},{label:'Depends on element type',correct:false}] },

  // ── MID ──────────────────────────────────────────────────────────────────
  { id:'css-m1', experienceLevel:'mid', type:'conceptual', question:'What are CSS Cascade Layers (@layer)?', answer:'@layer creates explicit specificity tiers. Rules in lower layers always lose to higher layers regardless of selector specificity, eliminating !important battles in design systems. Syntax: @layer reset, base, components, utilities. Unlayered styles beat all layered styles.', code:'@layer reset, base, components, utilities;\n@layer reset { * { margin: 0; box-sizing: border-box; } }\n@layer components { .btn { padding: 8px 16px; } }' },
  { id:'css-m2', experienceLevel:'mid', type:'conceptual', question:'What is CSS containment and what are the four types?', answer:'contain property isolates element rendering: size (element does not depend on children for sizing), layout (descendants do not affect outside layout), style (counter scoping), paint (descendants do not paint outside the element). content=layout paint is the most common combo. Improves rendering performance of large pages.' },
  { id:'css-m3', experienceLevel:'mid', type:'practical', question:'How do you build a CSS-only sticky header with a frosted glass effect?', answer:'Sticky positioning + backdrop-filter for glassmorphism.', code:'header {\n  position: sticky;\n  top: 0;\n  z-index: 100;\n  background: rgba(255,255,255,0.7);\n  backdrop-filter: blur(12px) saturate(180%);\n  -webkit-backdrop-filter: blur(12px) saturate(180%);\n  border-bottom: 1px solid rgba(0,0,0,0.08);\n}' },
  { id:'css-m4', experienceLevel:'mid', type:'conceptual', question:'What is CSS Houdini and what problem does it solve?', answer:'Houdini exposes browser rendering engine hooks to JavaScript. APIs: Paint Worklet (custom CSS images), Layout Worklet (custom layout algorithms), Animation Worklet (compositor-threaded animations), Typed OM (type-safe CSS in JS). Solves the problem of polyfilling CSS features that previously required DOM manipulation and JS overhead.' },
  { id:'css-m5', experienceLevel:'mid', type:'conceptual', question:'Explain the difference between logical properties and physical properties in CSS.', answer:'Physical: margin-left, padding-top — depend on screen direction. Logical: margin-inline-start, padding-block-start — depend on writing-mode and direction. Use logical properties for proper RTL support and vertical writing modes. Example: margin-inline (= left+right in LTR) instead of margin-left + margin-right.' },
  { id:'css-m6', experienceLevel:'mid', type:'output', question:'What is the output? Does margin collapsing apply?', code:'.parent { margin-top: 30px; }\n.child { margin-top: 20px; }\n<!-- No padding or border on parent -->', output:'Parent and child both get a 30px top margin (the larger one). The child margin collapses into the parent margin — only one margin appears.', explanation:'Margin collapsing: adjacent vertical margins on parent+first-child merge when no border/padding separates them. Adding padding-top:1px to parent prevents collapse.', choices:[{label:'30px total (larger margin wins, collapse)',correct:true},{label:'50px (both add up)',correct:false},{label:'20px (child overrides)',correct:false},{label:'0px (they cancel)',correct:false}] },
  { id:'css-m7', experienceLevel:'mid', type:'conceptual', question:'What are CSS subgrid and how is it different from a nested grid?', answer:'A nested grid creates its own independent grid tracks. subgrid (grid-template-columns:subgrid) makes a child participate in the PARENT grid tracks, enabling perfect column alignment between nested components. Crucial for design systems where you want card content to align across sibling cards.' },
  { id:'css-m8', experienceLevel:'mid', type:'practical', question:'How do you create a smooth scroll-driven animation using just CSS?', answer:'CSS scroll-driven animations (2023) animate based on scroll position without JavaScript.', code:'@keyframes fade-in {\n  from { opacity: 0; transform: translateY(30px); }\n  to   { opacity: 1; transform: translateY(0); }\n}\n.card {\n  animation: fade-in linear;\n  animation-timeline: view();\n  animation-range: entry 0% entry 30%;\n}' },
  { id:'css-m9', experienceLevel:'mid', type:'conceptual', question:'What is the performance difference between CSS Grid and absolute positioning for complex layouts?', answer:'Absolute positioning removes elements from flow — the browser cannot use them for layout calculations, but calculating each position independently is expensive for many elements. Grid uses layout algorithms that compute positions together efficiently. Grid typically outperforms absolute positioning for grid-like layouts and is more maintainable.' },
  { id:'css-m10', experienceLevel:'mid', type:'conceptual', question:'How do you implement container queries and when are they better than media queries?', answer:'Container queries style an element based on its CONTAINER size, not the viewport. Use when the same component appears in different column widths. A card inside a 3-col grid vs 1-col should respond to its container, not the page width.', code:'.sidebar { container-type: inline-size; }\n@container (min-width: 400px) {\n  .card { grid-template-columns: 1fr 2fr; }\n}' },
  { id:'css-m11', experienceLevel:'mid', type:'conceptual', question:'What is the @property rule in CSS and how does it enable animated custom properties?', answer:'@property registers a custom property with a type, initial value, and inheritance flag. This lets the browser interpolate between values during animation — otherwise custom property changes are not interpolatable. Enables smooth gradient transitions, numeric animations.', code:'@property --angle {\n  syntax: "<angle>";\n  initial-value: 0deg;\n  inherits: false;\n}\n@keyframes spin { to { --angle: 360deg; } }' },
  { id:'css-m12', experienceLevel:'mid', type:'practical', question:'How do you achieve CSS-only smooth animations that do not block the main thread?', answer:'Use properties that the compositor can animate without involving layout and paint: transform and opacity. Promote elements that will animate with will-change.', code:'/* ✅ Compositor-only (smooth) */\n.modal {\n  transform: translateY(100%);\n  opacity: 0;\n  transition: transform 0.3s ease, opacity 0.3s ease;\n}\n.modal.open {\n  transform: translateY(0);\n  opacity: 1;\n}\n/* ❌ Triggers layout (janky) */\n/* .modal { top: 100%; transition: top 0.3s; } */' },
];
