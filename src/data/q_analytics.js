// Data Analytics Tools Questions: Pandas, NumPy, Tableau, Power BI, Excel
// 12 fresher, 12 junior, 12 mid for each where possible, or combined appropriately to scale up.

export const ANALYTICS_QUESTIONS = [
  // ── PANDAS & NUMPY ────────────────────────────────────────────────────────
  // FRESHER
  { id:'pd-f1', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between a Pandas DataFrame and a Series?', answer:'A Series is a one-dimensional labeled array. A DataFrame is a two-dimensional tabular data structure with labeled axes (rows and columns) — essentially a dict of Series.' },
  { id:'pd-f2', experienceLevel:'fresher', type:'conceptual', question:'How do you handle missing values in Pandas?', answer:'Use df.isnull() or df.isna() to find them. df.dropna() removes rows/columns with missing values. df.fillna(value) replaces them with a specified value (like mean, median, or 0).' },
  { id:'pd-f3', experienceLevel:'fresher', type:'output', question:'What does df.head(3) do?', code:'print(df.head(3))', output:'Prints the first 3 rows of the DataFrame.', explanation:'.head(n) returns the first n rows. .tail(n) returns the last n rows. Default n is 5.', choices:[{label:'First 3 rows',correct:true},{label:'Last 3 rows',correct:false},{label:'Top 3 highest values',correct:false},{label:'Error',correct:false}] },
  { id:'np-f1', experienceLevel:'fresher', type:'conceptual', question:'What is NumPy and why is it faster than Python lists?', answer:'NumPy is a core library for scientific computing in Python. It provides the ndarray object. It is faster because arrays are stored in contiguous memory blocks, and operations are implemented in C (vectorized), avoiding Python loop overhead.' },
  { id:'pd-f4', experienceLevel:'fresher', type:'practical', question:'How do you select a single column from a DataFrame?', answer:'', code:"# Dictionary-like notation (preferred for columns with spaces):\ncol = df['column_name']\n# Attribute notation (only if name is a valid Python identifier):\ncol = df.column_name" },
  { id:'np-f2', experienceLevel:'fresher', type:'output', question:'What is the output?', code:'import numpy as np\narr = np.array([1, 2, 3])\nprint(arr * 2)', output:'[2 4 6]', explanation:'NumPy broadcasts the multiplication to every element in the array.', choices:[{label:'[2 4 6]',correct:true},{label:'[1, 2, 3, 1, 2, 3]',correct:false},{label:'Error',correct:false},{label:'2',correct:false}] },
  { id:'pd-f5', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between .loc and .iloc in Pandas?', answer:'.loc is label-based indexing (select by column/row names). .iloc is integer-position based (select by index 0, 1, 2). Slicing with .loc includes the endpoint; .iloc excludes it.' },
  { id:'pd-f6', experienceLevel:'fresher', type:'conceptual', question:'How do you read a CSV file into a Pandas DataFrame?', answer:'Use pandas.read_csv("filepath.csv"). You can also pass parameters like sep, header, names, and index_col.' },
  { id:'np-f3', experienceLevel:'fresher', type:'practical', question:'How do you create an array of 10 zeros in NumPy?', answer:'', code:'import numpy as np\nzeros = np.zeros(10)\n# Default type is float64. For integers:\nzeros_int = np.zeros(10, dtype=int)' },
  { id:'pd-f7', experienceLevel:'fresher', type:'output', question:'What does df.shape return?', code:'print(df.shape)', output:'A tuple (rows, columns)', explanation:'shape is an attribute, not a method, returning the dimensions of the DataFrame.', choices:[{label:'Tuple (rows, columns)',correct:true},{label:'List of row counts',correct:false},{label:'Total number of cells',correct:false},{label:'Returns null',correct:false}] },
  { id:'np-f4', experienceLevel:'fresher', type:'conceptual', question:'What is the shape of a NumPy array?', answer:'The shape is a tuple indicating the size of the array in each dimension. e.g., a 2D array with 3 rows and 4 columns has shape (3, 4).' },
  { id:'pd-f8', experienceLevel:'fresher', type:'practical', question:'How do you get descriptive statistics for numerical columns in a DataFrame?', answer:'Use the .describe() method.', code:'stats = df.describe()\n# Returns count, mean, std, min, 25%, 50%, 75%, max' },

  // JUNIOR
  { id:'pd-j1', experienceLevel:'junior', type:'conceptual', question:'How does the groupby() method work in Pandas?', answer:'It utilizes a split-apply-combine strategy. First, it splits data into groups based on key(s). Then, it applies an aggregation (like sum, mean) or transformation to each group. Finally, it combines the results into a new DataFrame/Series.' },
  { id:'np-j1', experienceLevel:'junior', type:'conceptual', question:'What is broadcasting in NumPy?', answer:'Broadcasting is how NumPy treats arrays with different shapes during arithmetic operations. The smaller array is "broadcast" across the larger array so that they have compatible shapes, without making unnecessary copies of data in memory.' },
  { id:'pd-j2', experienceLevel:'junior', type:'practical', question:'How do you merge two DataFrames in Pandas?', answer:'Use pd.merge().', code:"merged_df = pd.merge(df1, df2, on='common_column', how='inner')\n# how can be 'inner', 'outer', 'left', 'right'" },
  { id:'pd-j3', experienceLevel:'junior', type:'output', question:'What does this code do?', code:"df['new_col'] = df['A'].apply(lambda x: x * 2)", output:'Creates a new column "new_col" where every value in column "A" is multiplied by 2.', explanation:'.apply() runs a function on each element (or row/col). However, simple arithmetic should be vectorized (df["A"] * 2) instead of apply for better performance.', choices:[{label:'Multiplies column A by 2',correct:true},{label:'Adds 2 to column A',correct:false},{label:'Filters the DataFrame',correct:false},{label:'Throws an error',correct:false}] },
  { id:'np-j2', experienceLevel:'junior', type:'practical', question:'How do you reshape a NumPy array?', answer:'Use the .reshape() method.', code:'arr = np.arange(1, 10)  # 1D array of 9 elements\nreshaped = arr.reshape(3, 3)  # 2D array: 3 rows, 3 cols\n# Using -1 infers the dimension:\nreshaped = arr.reshape(-1, 3)' },
  { id:'pd-j4', experienceLevel:'junior', type:'conceptual', question:'What is the difference between merge, join, and concat in Pandas?', answer:'merge: SQL-like joins on specific columns. join: Combines DataFrames based on their indexes. concat: Appends DataFrames along an axis (vertically stack or horizontally append).' },
  { id:'np-j3', experienceLevel:'junior', type:'output', question:'Can you add a scalar to a NumPy array?', code:'arr = np.array([1, 2, 3])\nprint(arr + 5)', output:'[6 7 8]', explanation:'Yes, due to broadcasting, the scalar 5 is added to every element.', choices:[{label:'[6 7 8]',correct:true},{label:'Error',correct:false},{label:'[1, 2, 3, 5]',correct:false},{label:'[5, 10, 15]',correct:false}] },
  { id:'pd-j5', experienceLevel:'junior', type:'practical', question:'How do you find unique values and their frequencies in a Pandas column?', answer:'Use .value_counts().', code:"counts = df['category'].value_counts()\n# For just unique values:\nuniques = df['category'].unique()\n# For number of unique values:\nnum_uniques = df['category'].nunique()" },
  { id:'pd-j6', experienceLevel:'junior', type:'conceptual', question:'How do you handle datetime data in Pandas?', answer:'Use pd.to_datetime() to convert columns to datetime objects. Then you can access parts using the .dt accessor (e.g., df["date"].dt.year, df["date"].dt.month).' },
  { id:'np-j4', experienceLevel:'junior', type:'practical', question:'How do you find the index of the maximum value in a NumPy array?', answer:'Use np.argmax().', code:'arr = np.array([10, 30, 20])\nidx = np.argmax(arr)\n# idx is 1' },
  { id:'pd-j7', experienceLevel:'junior', type:'output', question:'What happens if you dropna(axis=1)?', code:'df.dropna(axis=1)', output:'Drops any COLUMN that contains at least one NaN value.', explanation:'axis=0 drops rows, axis=1 drops columns. Always be careful not to drop entire columns just for a few missing values.', choices:[{label:'Drops columns with any NaN',correct:true},{label:'Drops rows with any NaN',correct:false},{label:'Fills NaN with 1',correct:false},{label:'Error',correct:false}] },
  { id:'pd-j8', experienceLevel:'junior', type:'conceptual', question:'What is the use of the inplace=True parameter?', answer:'It applies the operation (like drop, fillna) directly to the existing DataFrame in memory, rather than creating and returning a new DataFrame. Modifies the original object.' },

  // MID
  { id:'pd-m1', experienceLevel:'mid', type:'conceptual', question:'What is vectorization in Pandas and why is it preferred over loops?', answer:'Vectorization performs operations on entire arrays (columns) simultaneously at the C level, bypassing Python\'s interpreter loop overhead. df["A"] + df["B"] is vastly faster than using for index, row in df.iterrows().' },
  { id:'np-m1', experienceLevel:'mid', type:'conceptual', question:'How does NumPy handle copy vs view?', answer:'Slicing an array returns a "view" (shares memory with original). Fancy indexing (using arrays of indices) returns a "copy". Modifying a view changes the original array; modifying a copy does not. Use .copy() to force a copy.' },
  { id:'pd-m2', experienceLevel:'mid', type:'practical', question:'How do you pivot a DataFrame?', answer:'Use pivot_table for handling duplicates (with aggregation) or pivot if unique.', code:"pivot_df = df.pivot_table(values='Sales', index='Region', columns='Year', aggfunc='sum')" },
  { id:'pd-m3', experienceLevel:'mid', type:'output', question:'What is the result of using groupby with transorm?', code:"df['avg_salary_dept'] = df.groupby('dept')['salary'].transform('mean')", output:'A series the same length as the original DataFrame, where each row contains the average salary of its respective department.', explanation:'.transform() returns results aligned with the original indices, unlike .agg() which returns reduced results.', choices:[{label:'Series same length as df with group means',correct:true},{label:'A reduced dataframe grouped by dept',correct:false},{label:'Error',correct:false},{label:'The global mean salary',correct:false}] },
  { id:'np-m2', experienceLevel:'mid', type:'practical', question:'How do you stack arrays vertically and horizontally in NumPy?', answer:'np.vstack() and np.hstack()', code:'import numpy as np\na = np.array([1, 2])\nb = np.array([3, 4])\nv = np.vstack((a, b))  # [[1, 2], [3, 4]]\nh = np.hstack((a, b))  # [1, 2, 3, 4]' },
  { id:'pd-m4', experienceLevel:'mid', type:'conceptual', question:'How to optimize memory usage when loading a huge CSV in Pandas?', answer:'1. Define datatypes explicitly (e.g., using smaller int types like int32 or float32). 2. Load only specific columns (usecols). 3. Convert categorical text columns to "category" dtype. 4. Read in chunks using chunksize parameter.' },
  { id:'pd-m5', experienceLevel:'mid', type:'practical', question:'How do you apply a custom aggregation function over multiple columns?', answer:'Use the .agg() method with a dictionary.', code:"grouped = df.groupby('City').agg({\n  'Revenue': ['sum', 'mean'],\n  'Customers': 'nunique',\n  'Rating': lambda x: x.max() - x.min()\n})" },
  { id:'np-m3', experienceLevel:'mid', type:'output', question:'What happens when you multiply two arrays using *?', code:'a = np.array([1, 2])\nb = np.array([3, 4])\nprint(a * b)', output:'[3 8]', explanation:'The * operator performs element-wise multiplication (Hadamard product). To perform matrix multiplication, use np.dot(a, b) or the @ operator (a @ b).', choices:[{label:'[3 8]',correct:true},{label:'11 (dot product)',correct:false},{label:'Error',correct:false},{label:'[[3, 4], [6, 8]]',correct:false}] },
  { id:'pd-m6', experienceLevel:'mid', type:'conceptual', question:'What is the difference between pivot_table and melt?', answer:'They are inverse operations. pivot_table converts "long" format data (many rows) to "wide" format (many columns). melt converts "wide" format data to "long" format by unpivoting columns into a single Variable-Value pair.' },
  { id:'pd-m7', experienceLevel:'mid', type:'practical', question:'How do you deal with shifting data, like calculating day-over-day difference?', answer:'Use the .shift() function.', code:"df['prev_day_sales'] = df['sales'].shift(1)\ndf['dod_diff'] = df['sales'] - df['prev_day_sales']\n# Use pct_change() for percentages:\ndf['dod_growth'] = df['sales'].pct_change()" },
  { id:'np-m4', experienceLevel:'mid', type:'conceptual', question:'What are NumPy structured arrays?', answer:'Structured arrays allow elements to have compound C-like data types (e.g., varying types for different fields, similar to rows in a table). They are the foundation of Pandas DataFrames but are harder to manipulate directly.' },
  { id:'pd-m8', experienceLevel:'mid', type:'output', question:'How does iterrows() compare to itertuples()?', code:'for row in df.itertuples(): pass', output:'itertuples() is significantly faster than iterrows().', explanation:'itertuples() returns namedtuples and preserves data types, avoiding the overhead of creating Pandas Series for each row like iterrows() does. Vectorization is better than both.', choices:[{label:'itertuples is much faster',correct:true},{label:'iterrows is much faster',correct:false},{label:'They are identical in speed',correct:false},{label:'Both are fully vectorized',correct:false}] },

  // ── TABLEAU & POWER BI ────────────────────────────────────────────────────
  // FRESHER
  { id:'tab-f1', experienceLevel:'fresher', type:'conceptual', question:'What is Tableau and what is it used for?', answer:'Tableau is a powerful Data Visualization and Business Intelligence tool used to convert raw data into understandable, interactive dashboards and worksheets without programming.' },
  { id:'pbi-f1', experienceLevel:'fresher', type:'conceptual', question:'What is Power BI?', answer:'Power BI is a Microsoft business analytics service that provides interactive visualizations and BI capabilities, allowing users to create reports and dashboards from various data sources.' },
  { id:'tab-f2', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between Dimensions and Measures in Tableau?', answer:'Dimensions (blue) contain qualitative, categorical data (e.g., Dates, Names, Regions) used to segment data. Measures (green) contain quantitative, numerical data (e.g., Sales, Profit) that can be aggregated (SUM, AVG).' },
  { id:'pbi-f2', experienceLevel:'fresher', type:'conceptual', question:'What are the main components of Power BI?', answer:'Power BI Desktop (authoring reports), Power BI Service (cloud sharing and collaboration), Power BI Mobile (viewing), and Power BI Gateway (syncing on-premise data to cloud).' },
  { id:'tab-f3', experienceLevel:'fresher', type:'conceptual', question:'What is a Tableau Dashboard?', answer:'A dashboard is a collection of several views (worksheets) arranged on a single canvas, allowing users to look at multiple data facets simultaneously and interact with them.' },
  { id:'pbi-f3', experienceLevel:'fresher', type:'conceptual', question:'What is DAX?', answer:'DAX (Data Analysis Expressions) is a formula language used in Power BI, Analysis Services, and Power Pivot in Excel. It is used to perform custom calculations on tables, columns, and data models.' },
  { id:'tab-f4', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between a Live connection and an Extract in Tableau?', answer:'Live connection continuously queries the database in real-time (good for real-time data but can be slow). Extract is a static snapshot (a .hyper file) saved into memory (faster but needs scheduled refreshes).' },
  { id:'pbi-f4', experienceLevel:'fresher', type:'conceptual', question:'What is Power Query?', answer:'Power Query is the data processing engine inside Power BI used to extract, transform, and load (ETL) data. You use the Power Query Editor to clean and shape data before loading it into the model.' },
  { id:'tab-f5', experienceLevel:'fresher', type:'conceptual', question:'What is a Filter in Tableau?', answer:'Filters are used to restrict the data being displayed. Common types include Extract filters (limits data brought into Tableau), Data Source filters, Dimension filters, and Measure filters.' },
  { id:'pbi-f5', experienceLevel:'fresher', type:'conceptual', question:'What does the visualization pane do in Power BI?', answer:'It allows you to choose the type of chart (bar chart, pie chart, map, matrix) and configure its properties, fields (Axis, Values, Legend), and formatting options.' },
  { id:'tab-f6', experienceLevel:'fresher', type:'conceptual', question:'What are the different types of joins in Tableau?', answer:'Inner, Left, Right, and Full Outer joins. Tableau also supports "Relationships" (the noodle line) which are dynamically queried, unlike standard hard-coded joins.' },
  { id:'pbi-f6', experienceLevel:'fresher', type:'conceptual', question:'How do you publish a report in Power BI?', answer:'You build the report in Power BI Desktop, then click the "Publish" button to upload it to a Workspace in the Power BI Service (cloud), where it can be shared via dashboards and apps.' },

  // JUNIOR
  { id:'tab-j1', experienceLevel:'junior', type:'conceptual', question:'What is the Order of Operations (Filter Execution) in Tableau?', answer:'1. Extract Filters, 2. Data Source Filters, 3. Context Filters, 4. Top N / Conditional Filters, 5. Dimension Filters, 6. Measure / Aggregation Filters, 7. Table Calculation Filters.' },
  { id:'pbi-j1', experienceLevel:'junior', type:'conceptual', question:'What is the difference between a Calculated Column and a Measure in DAX?', answer:'Calculated Column: Computed row-by-row during data refresh, takes up memory. Measure: Computed on-the-fly based on report filters (filter context), does not use extra file size. Use Measures for aggregations (ratios, sums).' },
  { id:'tab-j2', experienceLevel:'junior', type:'conceptual', question:'What is a Context Filter in Tableau?', answer:'By standard, all dimension filters are independent. A Context Filter forces Tableau to process it first, creating a temporary subset of the data. Sequential filters then only process data passing through the context filter.' },
  { id:'pbi-j2', experienceLevel:'junior', type:'conceptual', question:'What is the CALCULATE function in DAX?', answer:'CALCULATE(Expression, Filter1, Filter2) is the most powerful DAX function. It evaluates an expression in a modified filter context. It overrides or adds to existing filters from the visual.' },
  { id:'tab-j3', experienceLevel:'junior', type:'conceptual', question:'What is the difference between discrete and continuous fields in Tableau?', answer:'Discrete (blue) fields create headers/labels and separate data into finite buckets (e.g., individual Months). Continuous (green) fields create axes and represent an unbroken range of values (e.g., a continuous Timeline).' },
  { id:'pbi-j3', experienceLevel:'junior', type:'conceptual', question:'What is a Star Schema in Power BI?', answer:'It is the recommended data modeling approach where you have a central Fact table (transactions, sales) surrounded by Dimension tables (lookup tables like Date, Product, Customer) using 1-to-Many relationships.' },
  { id:'tab-j4', experienceLevel:'junior', type:'conceptual', question:'What is a Table Calculation in Tableau?', answer:'A transformation applied to the values in a visualization. They are computed locally after data is returned from the source. Examples: Running Total, Percent of Total, Difference From.' },
  { id:'pbi-j4', experienceLevel:'junior', type:'conceptual', question:'What is Row Context vs Filter Context in DAX?', answer:'Filter Context is the set of filters applied by the report (slicers, axes). Row Context exists when iterating over a table row-by-row (e.g., inside calculated columns or iterator functions like SUMX).' },
  { id:'tab-j5', experienceLevel:'junior', type:'practical', question:'How do you create a Dual Axis chart in Tableau?', answer:'Drag two measures to the Rows shelf. Right-click the second measure pill and select "Dual Axis". Remember to right-click the axis and choose "Synchronize Axis" if comparing similar units.' },
  { id:'pbi-j5', experienceLevel:'junior', type:'conceptual', question:'What is the difference between SUM and SUMX?', answer:'SUM aggregates a single column based on filter context. SUMX is an iterator: SUMX(Table, Expression) loops through a table row-by-row, computes the expression, and then sums the results up. Example: SUMX(Sales, Sales[Qty] * Sales[Price]).' },
  { id:'tab-j6', experienceLevel:'junior', type:'conceptual', question:'What are Tableau Parameters?', answer:'Dynamic values (like numbers, strings, or dates) that can replace constant values in calculations, filters, and reference lines. Users interact with a parameter control to change its value.' },
  { id:'pbi-j6', experienceLevel:'junior', type:'conceptual', question:'What does the DIVIDE function do in DAX?', answer:'DIVIDE(Numerator, Denominator, AlternateResult) performs safe division. It automatically catches division by zero or blank errors and returns the alternate result (or BLANK) instead of breaking the visual.' },

  // MID
  { id:'tab-m1', experienceLevel:'mid', type:'conceptual', question:'What are Level of Detail (LOD) Expressions in Tableau?', answer:'LOD expressions compute aggregations that are not tied to the visualization\'s level of detail. Types: FIXED (independent of view), INCLUDE (compute using view + specific dims), EXCLUDE (ignore specific dims).' },
  { id:'pbi-m1', experienceLevel:'mid', type:'conceptual', question:'How does Context Transition work in DAX?', answer:'Context Transition happens when a row context is converted into an equivalent filter context. It is triggered automatically when a measure is called inside an iterator, or explicitly by using CALCULATE within an iterator.' },
  { id:'tab-m2', experienceLevel:'mid', type:'practical', question:'Write a FIXED LOD expression to find the date of a customer\'s first purchase.', answer:'', code:'{ FIXED [Customer ID] : MIN([Order Date]) } \n// This finds the minimum date per customer, regardless of what is on the view.' },
  { id:'pbi-m2', experienceLevel:'mid', type:'practical', question:'Write a DAX measure to calculate Year-to-Date (YTD) Sales.', answer:'', code:'Sales YTD = TOTALYTD([Total Sales], \'Date\'[Date])\n// Or using CALCULATE:\nSales YTD = CALCULATE([Total Sales], DATESYTD(\'Date\'[Date]))' },
  { id:'tab-m3', experienceLevel:'mid', type:'conceptual', question:'How do you optimize a slow-performing Tableau Dashboard?', answer:'1. Use Extracts instead of Live. 2. Filter data early (Data Source/Extract filters). 3. Hide unused columns. 4. Reduce number of marks. 5. Prefer integer/boolean calculations over string manipulation. 6. Avoid overly nested LODs.' },
  { id:'pbi-m3', experienceLevel:'mid', type:'conceptual', question:'What is the VertiPaq engine?', answer:'VertiPaq is the underlying in-memory columnar database engine in Power BI. It heavily compresses data by dictionary encoding and value encoding. Because it stores data by column, aggregating millions of rows is extremely fast.' },
  { id:'tab-m4', experienceLevel:'mid', type:'conceptual', question:'What is the difference between blending and joining in Tableau?', answer:'Joining combines tables at the row level BEFORE aggregation (creates a single record set). Blending queries data sources independently, aggregates them, and THEN combines the aggregated results using a linking field.' },
  { id:'pbi-m4', experienceLevel:'mid', type:'conceptual', question:'How do you optimize a Power BI data model?', answer:'1. Adopt a Star Schema. 2. Remove columns/tables not needed (especially high cardinality text). 3. Disable Auto Date/Time and use a custom Date table. 4. Use Measures instead of Calculated Columns. 5. Split Date/Time columns into separate Date and Time columns.' },
  { id:'tab-m5', experienceLevel:'mid', type:'conceptual', question:'What is a Tableau Story?', answer:'A Story is a sequence of visualizations (sheets or dashboards) that work together to convey information interactively. Each point in a story is called a "story point".' },
  { id:'pbi-m5', experienceLevel:'mid', type:'conceptual', question:'What is the difference between ALL, ALLEXCEPT, and ALLSELECTED?', answer:'ALL: Removes all filters from a table/column. ALLEXCEPT: Removes all filters from a table EXCEPT on the specified columns. ALLSELECTED: Removes filters generated inside the query, but keeps explicit external filters (like user slicers).' },
  { id:'tab-m6', experienceLevel:'mid', type:'conceptual', question:'Can you explain how to create a Sets in Tableau and how they differ from Groups?', answer:'Groups statically combine categorical members (e.g., Group "A", "B" into "Category 1"). Sets are custom fields that define a subset of data dynamically based on condition (e.g., Top 10 Customers by Sales) or statically.' },
  { id:'pbi-m6', experienceLevel:'mid', type:'conceptual', question:'What is Time Intelligence in DAX and what are the prerequisites?', answer:'Functions that manipulate data over time periods (SAMEPERIODLASTYEAR, PARALLELPERIOD). Prerequisite: You MUST have a dedicated Date Table marked as a "Date Table", containing contiguous dates with no gaps.' },

  // ── EXCEL ─────────────────────────────────────────────────────────────────
  // FRESHER
  { id:'xl-f1', experienceLevel:'fresher', type:'conceptual', question:'What is the difference between relative, absolute, and mixed cell references?', answer:'Relative (A1): changes when copied. Absolute ($A$1): fixed, does not change when copied. Mixed ($A1 or A$1): locks either the column or the row.' },
  { id:'xl-f2', experienceLevel:'fresher', type:'practical', question:'What does the VLOOKUP function do?', answer:'VLOOKUP searches for a value in the first column of a table array and returns a value in the SAME ROW from another column.', code:'=VLOOKUP(lookup_value, table_array, col_index_num, [range_lookup])\n# Use FALSE or 0 for exact match.' },
  { id:'xl-f3', experienceLevel:'fresher', type:'conceptual', question:'What is Conditional Formatting?', answer:'A feature that changes the visual styling of a cell (color, icon, data bar) automatically based on its value or a custom formula.' },
  { id:'xl-f4', experienceLevel:'fresher', type:'conceptual', question:'What is a Pivot Table?', answer:'A powerful tool to calculate, summarize, and analyze tabular data. You can drag and drop fields into Rows, Columns, Values, and Filters to quickly generate reports.' },
  
  // JUNIOR
  { id:'xl-j1', experienceLevel:'junior', type:'practical', question:'Why is INDEX MATCH preferred over VLOOKUP?', answer:'1. It can look up values to the left (VLOOKUP only looks right). 2. Inserting/deleting columns does not break it (VLOOKUP uses hardcoded column index). 3. Faster on large files.', code:'=INDEX(return_range, MATCH(lookup_value, lookup_range, 0))' },
  { id:'xl-j2', experienceLevel:'junior', type:'practical', question:'How do XLOOKUP and VLOOKUP differ?', answer:'XLOOKUP (newer) can look both left and right, defaults to exact match, allows searching from the bottom up, and has built-in error handling (if_not_found).', code:'=XLOOKUP(lookup_value, lookup_array, return_array, [if_not_found])' },
  { id:'xl-j3', experienceLevel:'junior', type:'conceptual', question:'What is the difference between COUNT, COUNTA, and COUNTIF?', answer:'COUNT counts cells containing numbers. COUNTA counts non-empty cells (text or numbers). COUNTIF counts cells that meet a specific condition.' },
  { id:'xl-j4', experienceLevel:'junior', type:'practical', question:'How do you use SUMIFS?', answer:'SUMIFS sums cells that meet multiple criteria. Unlike SUMIF, the sum_range is the FIRST argument.', code:'=SUMIFS(sum_range, criteria_range1, criteria1, [criteria_range2, criteria2], ...)' },

  // MID
  { id:'xl-m1', experienceLevel:'mid', type:'conceptual', question:'How do you create an array formula?', answer:'In older Excel, press Ctrl+Shift+Enter (adds {}). In Microsoft 365, formulas naturally spill into adjacent cells (dynamic arrays). They perform multiple calculations on items in an array.' },
  { id:'xl-m2', experienceLevel:'mid', type:'conceptual', question:'What is Power Query in Excel?', answer:'An ETL (Extract, Transform, Load) tool used to connect to diverse data sources, clean and shape data without VBA, and load it into sheets or the Data Model. Steps are recorded and repeatable.' },
  { id:'xl-m3', experienceLevel:'mid', type:'conceptual', question:'What is the Excel Data Model (Power Pivot)?', answer:'A relational engine inside Excel that allows linking multiple tables, overcoming the 1 million row limit, and writing advanced DAX measures instead of standard Excel formulas.' },
  { id:'xl-m4', experienceLevel:'mid', type:'practical', question:'How do you remove duplicates using a formula in Excel 365?', answer:'Use the UNIQUE function for dynamic arrays.', code:'=UNIQUE(A2:A100)' },
];
